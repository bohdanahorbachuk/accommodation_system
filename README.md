# accommodation_system
🧩 Requirements

Node.js: v20.0.0 або новіша
(перевірити версію можна командою:)
node -v

Якщо потрібно оновити Node:
nvm install 20
nvm use 20

📦 Встановлення залежностей
npm install

▶️ Запуск проєкту локально
npm run dev

Після запуску відкрий у браузері:
http://localhost:5173/

🧰 Основні технології
React 18
Vite
Material UI
JavaScript / JSX
CSS


🚀 Посібник із запуску проєкту для колег
1. 📋 Локальні вимоги
Ваш проєкт використовує Node.js та npm (Node Package Manager) для керування залежностями.

Node.js: Версія v20.0.0 або новіша.

Як перевірити: node -v

Як оновити (якщо використовують nvm): nvm install 20 та nvm use 20

Git: Повинен бути встановлений для клонування репозиторію.

2. 📁 Команди для запуску
Ваші колеги мають виконати ці кроки у терміналі:

Крок 1: Клонування репозиторію
Якщо проєкт знаходиться у Git-репозиторії (наприклад, GitHub, GitLab):

Bash

git clone [адреса_вашого_репозиторію]
Bash

cd accommodation_system-1
Крок 2: Встановлення залежностей
Ця команда зчитує файл package.json і встановлює усі необхідні бібліотеки (React, Vite, Material UI, тощо) у папку node_modules.

Bash

npm install
Крок 3: Локальний запуск проєкту
Ця команда викликає скрипт "dev" із вашого файлу package.json, який запускає сервер розробки Vite.

Bash

npm run dev
Крок 4: Перехід у браузері
Після успішного запуску (коли з'явиться повідомлення VITE vX.X.X ready), колеги можуть відкрити сторінку:

http://localhost:5173/
Загальний скрипт для швидкого старту:
Якщо вони клонують проєкт вперше:

Bash

git clone [адреса_вашого_репозиторію] && cd accommodation_system-1 && npm install && npm run dev
3. ⚠️ Важлива примітка щодо роутингу
Обов'язково повідомте колегам, що ви тимчасово відключили складну логіку маршрутизації (React Router) і зараз потік виглядає так:

За кореневим посиланням (http://localhost:5173/) одразу відкривається Форма (ApplicationForm).

Клік на "Подати заявку" → Сторінка підтвердження.

Клік на "Переглянути статус заявки" → Сторінка статусу.

Це дозволить їм перевірити всі три компоненти, не маючи справу з проблемою шляхів, яку ми щойно вирішили.
